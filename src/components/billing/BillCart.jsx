import{Trash2,Minus,Plus}from"lucide-react";export default function BillCart({items,onRemove,onIncrease,onDecrease}){if(!items.length)return <div className="empty-state"><p>No services added yet.</p></div>;return <div className="bill-cart">{items.map(i=><div className="cart-row" key={i.id}><div><strong>{i.name}</strong><small>{i.duration}</small></div><div className="qty-control"><button onClick={()=>onDecrease(i.id)}><Minus size={13}/></button><span>{i.qty}</span><button onClick={()=>onIncrease(i.id)}><Plus size={13}/></button></div><strong>₹{i.price*i.qty}</strong><button className="delete-button" onClick={()=>onRemove(i.id)}><Trash2 size={16}/></button></div>)}</div>;}