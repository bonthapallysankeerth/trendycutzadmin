import{useState}from"react";import PageHeader from"../../components/common/PageHeader";import Avatar from"../../components/common/Avatar";import{staff}from"../../data/mockData";export default function Attendance(){const[state,setState]=useState(Object.fromEntries(staff.map(s=>[s.id,"Present"])));return <><PageHeader title="Attendance" description="Track daily attendance of salon staff." action={<button className="button primary">Save Attendance</button>}/><section className="card attendance-card"><div className="attendance-header"><div><h3>Today</h3><p>{staff.length} staff members</p></div><button className="select-button">31 August 2026</button></div>{staff.map(m=><div className="attendance-row" key={m.id}><div className="customer-cell"><Avatar name={m.name}/><div><strong>{m.name}</strong><small>{m.role}</small></div></div><div className="attendance-actions">{["Present","Absent","Leave"].map(s=><button key={s} className={`attendance ${state[m.id]===s?"active":""}`} onClick={()=>setState({...state,[m.id]:s})}>{s}</button>)}</div></div>)}</section></>;}